export interface ButtonProps {
  title?: string;
  colorBg: string;
  colorText: string;
  // color: "mocha" | "strawberry" | "cremeBrulee" | "milk";
  routeLink: string;
  iconBefore?: string;
  iconAfter?: string;
}
